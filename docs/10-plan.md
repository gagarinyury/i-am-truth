# План до дедлайна

> Дедлайн: **01.09.2026, 02:00 CEST**. Файл создан 29.08 в 00:10, когда до него
> оставалось 73,8 ч. Задача файла — пережить потерю контекста: статусы правит тот,
> кто закрыл пункт, задним числом ничего не переписывается.

## Правило приоритета

Всё, что ниже, отсортировано так: **сначала то, без чего подача невозможна**
(S1, S7), потом то, что усиливает уже работающее (P1–P3), потом необязательное
(P4–P5). Если время кончается — режется снизу, а не сверху.

## Календарь

| Блок | Когда | Что | Жёсткость |
|---|---|---|---|
| A | 29.08 день | P1, P2, P3 → передеплой → перемер | можно двигать |
| B | 30.08 день | P4, P5, прогон обоих эталонов, правка доков | можно двигать |
| **Заморозка кода** | **30.08, 22:00** | после неё код не трогается — только доки и видео | **жёстко** |
| C | 31.08 с 06:00 | S1 — видео (порог «≤20 ч до дедлайна» наступает 31.08 в 06:00) | **жёстко, не раньше** |
| D | 31.08 до 20:00 | S7 — подача на Devpost | **жёстко, не позже** |
| буфер | 31.08 20:00 → 01.09 02:00 | 6 часов на сюрпризы | не занимать |

## Обязательное к подаче

- **S1 · Демо-видео** 🔲 — ≤4 мин, публично на YouTube/Vimeo, английский или
  английские субтитры. Сценарий готов: `docs/09-video-script.md`, кадры 2–4
  переписаны под браузер, кадр 5 — облако. Критерий готовности: ссылка открывается
  в приватном окне без входа в аккаунт.
- **S7 · Подача на Devpost** 🔲 — текст под копипаст лежит в `docs/08-submission.md`.
  Критерий: форма отправлена, на странице проекта видны ссылки на репозиторий,
  видео и живой сервис.
- Остальное (S2–S6) закрыто 28.08, перепроверить одним проходом в блоке B.

## Продукт

- ✅ **P1 · Сохранять одиночные прогоны** (29.08, ночь). Сейчас `pipeline.run` не пишет результат
  никуда — в GCS складывает только батч-джоб. Из-за этого разбор, сделанный через
  сайт, невоспроизводим: 28.08 обсуждали отчёт, которого нет ни в одном файле.
  Критерий: после прогона через `/analyze` в бакете лежит JSON, `/runs` его видит.
- ✅ **P2 · Вектор направлений считает код, а не модель** (29.08). Направление есть у каждого
  домена и есть общее, но общее называет модель. Свести домены в счёт
  (away / towards / unpredictable) должна функция — по тому же правилу, по которому
  ARR считает `stats_tool`. Критерий: в отчёте видно, сколько доменов тянет в какую
  сторону, и это воспроизводится из JSON без модели.
- ✅ **P3 · Экспорт одностраничного брифа** (29.08). Кнопка на странице → Markdown. Нужен
  судьям (минута вместо прокрутки) и видео. Критерий: файл открывается и читается
  без исходного JSON.
- ✅ **P4 · Ложные `GROUP_MISMATCH`** (29.08). Причина оказалась не той, что была
  записана в F-51: не три группы, а подстрока «control» внутри «glycemic control».
  13 случаев → 0, F-56.
- ✅ **P5 · `truth/grounding.py`** (29.08). Замерен перед решением: на полной статье
  `checkGrounding` даёт выдумке тот же балл, что и правде (0.003 против 0.003).
  В продукт не идёт, перенесён в `eval/probes/grounding_probe.py`, F-57.

- **P0 · «Посчитано функцией, а не моделью» было неправдой** (найдено и починено
  29.08 по дороге, F-55). `stats_tool` в прямом пути не вызывался ни разу; теперь
  считает `truth/recompute.py` и называет основание пересчёта.

## После любой правки кода

1. Три теста: `test_claim_labels.py`, `test_normalise_math.py`, `test_parallel_isolation.py`.
2. Прогон обоих эталонов (`mcdonald-2026` — свой, `cheng-2024` — внешний). Падение
   ниже прежней медианы = откат правки, а не «потом разберёмся».
3. Передеплой на Cloud Run (заодно подтянется название «Я Правда» — в развёрнутой
   ревизии 00010 ещё старое).
4. Новый факт `F-NN` в `docs/02-verified-facts.md` с командой и датой.

## Чего не делать

- Не менять промпт после заморозки кода: любая правка требует перемера обоих
  эталонов, а это часы.
- Не начинать видео раньше 31.08 06:00 — решение принято сознательно, чтобы снимать
  готовый продукт, а не промежуточный.
- Не занимать шестичасовой буфер работой «раз уж время есть».
